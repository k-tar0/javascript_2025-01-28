const bombedTarget = [ , ,
    "hospitals",
    "schools",
    "mosques",
    "churches",
];

const result = bombedTarget.map((target) =>
     `The ${target} were bombed`);

console.log(bombedTarget[0]);